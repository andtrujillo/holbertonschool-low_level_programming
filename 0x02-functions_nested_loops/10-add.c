#include "holberton.h"
/**
 * add - Function that adds two integers and returns the result
 * @fir_num: first number to add 
 * @sec_num: second number to add with first
 * Return: integer
 */
int add(int fir_num, int sec_num)
{
	int result = fir_num + sec_num;

	return (result);
}
